# Collaptz


# Set up the HTTP API

- pip install pip-tools
- pip install -r requirements.txt 
- docker
- docker-compose up -d db
- make migrate
- pip install -r requirements.txt
- make requirements.txt 
- make migrate
- pip install -r requirements.txt 
- make migrate
- make run 

